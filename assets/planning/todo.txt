================================================================================
#####----------------------------------------------------------------------#####
#       14 August 2026 - Agenda: Documentation, ASM code generation phase.     #
#####----------------------------------------------------------------------#####
================================================================================

Current planned eforts, in order:

1. Review and update project documentation.

    1.1. Go through current documentation, make sure nothing needs changing
         after the large elegance boosting refactoring effort.

    1.2. Write new documentation for the SSA IR generation phase.

    1.3. Write new documentation for calling the compiler on the command line.


2. Come up with an initial design for how to emit assembly code from SSA IR.

   Design questions: - How to represent assembly instructions from different
                       architectures. How to use OOP elegantly here. Perhaps
                       there's a class for instructions that perform ADD, one
                       for MUL instructions. Or perhaps there's a class for
                       instructions with 0 operands, 1, 2 operands, etc.

                       Think about how to elegantly design this part.

                     - How to map each Hirola language construct to a set of
                       known assembly instructions in a given architecture.
                       Like, an ADD IR instruction maps to ADD assembly code.
                       A loop maps to a cmp/test and a jne/je for example.

                     - When, how and where should register allocation be
                       performed even if not yet implemented in this effort now.

                     - How to go from assembly code to an ELF binary.

================================================================================
#####----------------------------------------------------------------------#####
#       30 July 2026 - Agenda: Creating the ToDo list, add initial content.    #
#####----------------------------------------------------------------------#####
================================================================================

Efforts that were completed before the To Do list existed:

    - Create the initial design of the language.
    - Get the Lexer working, constructing a correct Token Array.
    - Write documentation for the Lexer.
    - Get the Parser working - AST Nodes in custom cache-friendly memory arena.
    - Write documentation for the Parser & AST construction.
    - Start writing documentation for low-level optimizations in the project.
    - Get the SSA IR generator working, IR instructions in custom memory arena.

Current planned efforts, in order:

1. Do the first extensive code cleanup and elegance boost.

  [COMPLETED]
  1.1. Give important data structures & variables better names. Also, make sure
       they get the same name across all owning classes they'll be getting
       passed on to.

  [COMPLETED]
  1.2. Get rid of bad patterns:

      [COMPLETED: Fantastic reductions in lines of code.                      ]
      [           Parser was 600 lines, now 400. IR Generator 1200 -> 1000 LoC]
      1.2.1.
      Clumps of bookkeeping variables being passed around, along with the data
      structure they describe, instead of having an object for said structure,
      with member functions cleanly handling adding elements, finding an
      element, reallocating the data structure with more memory, etc.

      Four data structures come to mind that neede this improvement: The Code
      Block Directory, the Statement Directory, the IR Instructions Directory
      and the custom memory arena - a region where different sized, but related,
      objects are placed next to each other in memory. So far we have two:
        - AST Arena
        - IR Instructions Arena

      [COMPLETED]
      1.2.2.
      In class definitions, put the data members that won't get accessed by
      anything foreign to the class in private: sections.

      [COMPLETED]
      1.2.3.
      Repetitive code, for instance when emitting IR instructions for BinOps.

      [COMPLETED]
      1.2.4.
      Remove unused variables everywhere.

      [COMPLETED]
      1.2.5.
      Inconsistent ownership transfer when moving to the next compiler phase.
      Make all ownership of large data structures be transferred via move
      semantics. So far, at these borders: Lexing -> Parsing -> IR Generation.

  [COMPLETED]
  1.3. Establish a clean coding style, apply to all 3000 lines of existing code.

  [COMPLETED: A further 130 lines of code eliminated.]
  1.4. Reduce total lines of code as much as possible.

       - Get rid of unnecessary default destructor definitions.
       - Get rid of unnecessary comments.

       - Reduce long multi-line expressions and statements by saving parts of it
         into intermediate automatic variables. Like, in parser.h:

       (*statement_directory)
           [next_free_stmt_dir_entry + (*used_stmt_dir_entries)]
                = std::make_tuple
                    (block_dir_ix, statement_ix++, arena_offset_to_statement);

        there should be a helper variable to store the precomputed index neatly:

        size_t stmt_dir_ix;
        stmt_dir_ix = next_free_stmt_dir_entry + (*used_stmt_dir_entries);

        (*statement_directory)[stmt_dir_ix] = std::make_tuple
            (block_dir_ix, statement_ix++, arena_offset_to_statement);

        There, 4 lines reduced into 2 lines.

        - Make variable names shorter, e.g. index -> ix, directory -> dir
        - Where possible, put [[(un)likely]] blocks on same line as IF condition
        -

  [COMPLETED]
  1.5. Comments may need to be added, adjusted or reduced in some places.
       Go over the whole existing codebase and make sure all comments are OK.

[COMPLETED]
2. Come up with and implement a proper compiler driving program and stages.
   Source code should come from files.
